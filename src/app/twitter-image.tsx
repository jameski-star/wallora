// The Twitter/X share card reuses the same branded image as Open Graph.
// Re-exporting keeps a single source of truth (size, alt, contentType, render).
export { alt, size, contentType, default } from "./opengraph-image";
